language: ruby
cache: bundler
sudo: false
rvm: 
  - 2.2
before_install:
  - gem update --system
  - gem install bundler
before_script:
  - cp 'config/mongoid_sample.yml' 'config/mongoid.yml'
  - cp 'dotenv_example' '.env'
  - sleep 10
services:
  - mongodb

